Split vm_main.plsw (1000+ lines) into 6 PL/SW modules following snobol4 conventions: vm_regs (register access), vm_heap (cells, heap, deref, bind, trail), vm_choice (choice-point frames), vm_io (print, trace, atom table), vm_tests (test loaders), vm_main (init, dispatch, MAIN). Each library module has %DEFINE LIBRARY and dummy MAIN. Build command updated. Version bumped to v0.2. No behavioral changes.