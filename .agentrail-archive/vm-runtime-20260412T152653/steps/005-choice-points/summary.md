Added CP_PUSH (write 14-cell frame, overflow guard), CP_POP (unwind trail, reset HP, restore all registers, pop frame), CP_READ/CP_WRITE helpers. TRY pushes choice point with alt from immediate. TRUST pops and restores. BP convention: points to frame base (CP_BASE = sentinel for empty). Test 7: two-clause pred, query matches first clause via TRY path, BP shows choice point exists.